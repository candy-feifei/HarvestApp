现在需要生成一个工具，用于量化以及标准化AI录入时的prompt，
1.需要一个简单的界面用于展示标准化的promt模版
2.需要通过docker独立部署 
3.需要需要可以更改prompt模版。 
4.需要记录promt对话历史以及记录回馈信息。
5.需要将这个工具放到目录AI/tools中
6.我需要一个功能具备创建角色卡的功能，例如，当前人是张三，他的擅长的技能是react，vue，了解部分java，php语言。现在他需要做某件工作时，要根据他的技能树进行评估。
7.当我们录入一个任务目标时，如果启动了角色卡，分配原则，则会对相关角色进行工作安排，同时给出他们的技能树与工作的匹配度。
8.需要接入AI的API，例如元宝，千问，DEEPSEEK。询问记录时，可以选择prompt模版，然后与AI的对话记录需要存储起来，可以使用nosql存储。
9.需要创建一个project功能
  9.1 project可以具备自己的知识库。
  9.2 每一个project都应该有一个或多个角色担任不同的工作协调进行，例如有的负责后端开发，有的负责测试，有的负责前端开发，有的啥工作都能做。
  9.3 每一个project都应该有一份工作大纲，达成目的，工作细则。
  9.4 AI应该按照这份工作记录进行创建详细的工作策略。例如，输入基本资料，编辑好用户卡，制定好时间，拆解需求，AI完成粗略基础架构，生成文档，然后交付给不同的角色执行具体的细分工作。
  9.5 AI交互时应该提供打字稿效果
  9.6 同时交互应该应该能够删除
  9.7 生成的基础文稿应该具备良好的展示格式，可以以一个在线的md进行展示。同时，他应该进入聊天模式，可以与AI进行交互，以交互结果修正这个md，同时md应该具备版本。当需要修正MD时，需要交互时询问用户，用户也很可能只是想聊天而已。
  9.8 为了观测AI的生成以及回复效果，需要一个窗口在AI交互时，展示发送至AI接口时的prompt请求内容。
  9.9 AI assistant功能，当选择project时，他应该带上project的md作为基本知识库，在此基础上进行创造。
10.以英文为主。
11.主题风格以小清新式的风格为主。应该以左右布局为主，另外需要搭配将几项大类进行拆分组合，将相同的内容归为一类，他应该是以聊天为主，占用绝大多数界面才对，管理项，应该属于配置类。
12.将setting放置在左下角，conversation应该尽可能的大，message应该固定在页面下方。页面头部是不需要的

PS::
修复：
1. new chart无效。不会重置整个界面。
2. 修复现在打字稿状态下，展示内容是堆叠在一起的问题，他应该是具备格式的输出。