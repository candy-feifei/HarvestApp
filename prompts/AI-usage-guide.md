# HarvestApp 项目 AI 使用说明（团队协作版）

本文是本仓库的 **AI 使用说明**，用于比赛材料与团队协作统一口径。  
配套可复用 Prompt 模板位于 `prompts/`（见 [`prompts/README.md`](./README.md)）。

| 快速定位 | 路径 |
|----------|------|
| 本说明（你正在读） | `prompts/AI-usage-guide.md` |
| 模板目录与文件一览 | [`prompts/README.md`](./README.md) |
| 各模板正文 | `prompts/prd.md`、`nest-module.md`、`frontend-feature.md` 等 |

---

## 目录

1. [目标与边界](#1-目标与边界)  
2. [安全与脱敏](#2-安全与脱敏必须遵守)  
3. [推荐工作流](#3-推荐工作流最省返工)  
4. [项目结构约定](#4-本项目结构约定给-ai-的上下文要写清)  
5. [如何向 AI 提需求](#5-如何向-ai-提需求prompt-写法规范)  
6. [人工校验清单](#6-人工校验清单每次合入前至少过一遍)  
7. [产出物与比赛材料](#7-产出物与比赛材料如何对齐)  
8. [反模式与常见返工](#8-反模式与常见返工)  
9. [文档与模板的维护](#9-文档与模板的维护)  
10. [附录：极简示例 Prompt](#10-附录极简示例-prompt)  

---

## 1. 目标与边界

### 1.1 我们用 AI 做什么

- **需求与拆解**：把业务需求拆成可实现的任务、接口与验收标准。
- **工程化实现**：按仓库既有结构新增/修改模块（Nest module、React feature、DTO、API 调用、Query 缓存）。
- **测试与文档**：生成单测/E2E 思路与用例骨架；维护可复用的 `prompts/` 模板与 `docs/` 文档。
- **代码评审与复盘**：生成 Review checklist、PR 描述草稿、风险清单与答辩 Q&A。
- **复杂业务逻辑攻克（比赛加分向）**：辅助把非结构化规则/说明转成可执行的实现方案（例如复杂 RBAC、审批流、跨模块状态机），并以“验收标准 + 反例 + 边界”固化到文档与模板。
- **存量代码理解与复刻**：辅助快速阅读已有模块/页面，归纳关键数据流、权限点与缓存维度，降低“黑盒复刻”的试错成本。
- **工程质量反向约束**：在生成代码前明确分层与约束，让 AI 更稳定地产出可维护实现（避免 Controller 过胖、组件内乱写请求、Query key 过粗等）。

### 1.2 AI 不该替代的事情（必须人工拍板）

- **需求优先级与范围裁剪**：短周期下的 P0/P1 取舍。
- **权限与安全**：RBAC/数据隔离/越权风险，任何“代操作他人数据”必须严格审查。
- **数据模型与迁移策略**：表结构、索引、约束与回滚策略。
- **关键 UX 决策**：信息架构、关键流程是否符合目标用户。

---

## 2. 安全与脱敏（必须遵守）

### 2.1 禁止提交/粘贴的内容

- **密钥/Token/账号密码**：`.env`、JWT、数据库连接串、第三方 API key。
- **真实客户/个人隐私**：姓名、邮箱、电话、地址、合同、内网 URL、截图中可识别信息。
- **公司内部信息**：未公开业务、内部代号、服务器地址、日志原文（含敏感字段）。

### 2.2 允许提交的内容（推荐）

- **模板 + 占位符**：将真实信息替换为 `ACME_CORP`、`USER_A`、`TOKEN_XXX` 等。
- **已脱敏示例**：能展示 Prompt 结构与方法，但不含敏感数据。
- **PR/Commit 可对照的产出**：在 PR 描述中引用使用过的模板文件名与人工校验点。

### 2.3 在对话/工具里交给模型前（10 秒自检）

- 是否已去掉环境变量、Cookie、Authorization 头、连接串？  
- 堆栈/日志里是否带邮箱、用户 ID 以外的 PII？  
- 截图是否已打码或换示意图？

### 2.4 比赛特别说明（用于 Demo / 截图材料）

比赛材料需要“真实感”，但**仓库与对话内容**仍必须遵守脱敏。若必须展示更像真实企业数据的效果：

- 使用**虚构测试账号/虚构客户**（例如 `judge_test_user@company.com`、`ACME_CORP`）与生成的样例数据。
- 在视频/截图旁附 1 句说明：该数据为测试数据或已获授权（按实际情况描述）。
- 严禁在 Git 仓库中留存未脱敏生产数据（包括日志原文、截图、导出的 CSV/SQL）。

---

## 3. 推荐工作流（最省返工）

### 3.1 “先契约后实现”

1. 用 `prompts/prd.md` 产出：**P0/P1 功能清单 + 验收标准**。
2. 用 `prompts/nest-module.md` 明确：**API 路径、DTO 字段、错误码/异常文案、权限规则**（面向用户的错误信息须 **可读、不泄露实现细节**；团队若约定英文 UI/接口提示，在 PRD 中写清）。
3. 若需要，先跑通 **最小链路**（登录 → 列表 → 新增/编辑 → 审批/提交等）。
4. 最后再补 **UI 打磨 / 边界条件 / 测试**。

### 3.2 “小步快跑 + 可回滚”

- 每次改动尽量围绕一个明确目标：一个 API、一个页面、一个交互或一条测试链路。
- 任何影响范围大的改动，都要写清：  
  - **涉及文件**（路径列表）  
  - **风险点**（权限、缓存、数据迁移）  
  - **回滚方式**（撤销哪些提交/恢复哪些文件）

### 3.3 实现后最小验证（与 AI 协作时别省）

- 前端：在子项目下执行 `npm run build` 或 `npx tsc -b`，保证类型通过（具体脚本以 `frontend/package.json` 为准）。  
- 后端：在 `backend/` 下按项目脚本跑 build/test，避免合入后才发现类型或编译错误。  
- 有变更的接口：至少手测或在 PR 中说明用 curl/客户端如何验证。

---

## 4. 本项目结构约定（给 AI 的上下文要写清）

### 4.1 前端（`frontend/`）

- 领域按 `src/features/<domain>/` 组织；页面多在 `pages/`，接口在 `api.ts`。
- HTTP 统一封装在 `src/lib/api/http.ts`（`apiRequest` + `ApiError` + 401 处理）。
- 数据获取优先用 **TanStack Query**；Query key 必须包含影响数据的维度（如 week、view、scope、被查看用户等），避免 **缓存串数据**。

### 4.2 后端（`backend/`）

- NestJS 按模块组织：`src/modules/<domain>/...`。
- DTO 在模块内 `dto/`；通用能力在 `src/common/`（decorators/filters/utils/dto）。
- 任何“跨用户/代操作”能力必须：  
  - **显式参数**（query/header/body），不可隐式只依赖“看起来像当前用户”的侧信道  
  - **权限校验**（角色/能力）  
  - **组织成员校验**（同组织、状态合法等，以各模块实际规则为准）

### 4.3 在 Prompt 里建议同时给出的信息

- **具体路径**（如 `frontend/src/features/time/api.ts`）比只说“时间模块”更省轮次。  
- **已有类似实现**：指向可参考的文件/模块，降低风格漂移。  
- **不改动范围**（如“不要动迁移脚本/不要改 public API 路径”），减少无关重构。

---

## 5. 如何向 AI 提需求（Prompt 写法规范）

### 5.1 必填信息（建议复制粘贴）

- **目标**：一句话描述要达成的用户价值。  
- **范围**：影响哪些页面/API/模块（路径或模块名）。  
- **权限**：谁能看/谁能改/是否涉跨用户与组织隔离。  
- **验收标准**：至少 3 条，**可执行、可测**。  
- **约束**：不改哪些文件/须保持的兼容面（API、字段、行为）。
- **架构约束（强制建议写）**：在生成代码前明确项目分层规则，例如：  
  - **NestJS**：Controller 只做参数解析与权限入口；业务逻辑放 Service；Service 不直接操作 HTTP Response；DTO 放模块内 `dto/`；跨模块能力走清晰的 service 依赖或 common。  
  - **React**：组件不直接 `fetch`；统一走 `features/<domain>/api.ts` + TanStack Query；Query key 必须包含影响数据范围的维度（week/view/scope/user 等）。

### 5.2 有 bug 时额外附上

- **现状**：用户或日志里**脱敏**后的表现（无敏感 token/真实客户名）。  
- **期望**：一句话 + 边界（空数据、无权限、并发等若相关）。  
- **复现**：环境（本地/哪个分支）与最短步骤。  

### 5.3 推荐模板（直接用仓库文件）

- 需求/验收：`prompts/prd.md`  
- Nest 模块与接口：`prompts/nest-module.md`  
- 前端 Feature：`prompts/frontend-feature.md`  
- 测试：`prompts/testing.md`  
- 复盘/答辩：`prompts/review.md`  

> **重要提示（比赛期间）**：任何对 `prompts/*.md` 模板的修改，都必须在 PR 描述中说明**修改原因**与**带来的收益**（例如：“新增针对复杂位运算/协议字段校验的约束，减少幻觉字段与伪实现”）。评审会用它判断团队是否在**动态优化 AI 工作流**，而不是机械套用通用模板。

> 团队协作建议：PR 描述里写上 **使用的模板文件名 + 人工校验点**，便于复盘与比赛材料整理。

---

## 6. 人工校验清单（每次合入前至少过一遍）

### 6.1 通用

- [ ] 无敏感信息提交（`.env`、token、真实数据、截图敏感内容）  
- [ ] 路由/API/字段命名与现有代码风格一致  
- [ ] 错误信息对用户可读（**避免**直接暴露堆栈、内部表名/字段名/路径）  
- [ ] 关键逻辑有最小测试覆盖（单测或 E2E 或明确说明为何本次豁免）  
- [ ] 若产品/团队约定**英文（或其它固定语言）** UI/提示，新文案与现网一致、无未翻译残留  

### 6.2 权限与数据隔离（涉及团队/他人数据时必选）

- [ ] 非授权角色无法越权查看/修改他人或他组织数据  
- [ ] 任何“以某身份代操作/查看”的入口有明确权限与审计痕迹（按产品设计）  
- [ ] 前端 Query key 含**区分不同数据范围** 的因子，避免列表/详情串缓存  
- [ ] UI 对越权/只读/待审批等有清晰提示（如 timesheet 锁定、多入口冲突说明等）  

### 6.3 AI 纠错与迭代记录（比赛加分项：过程证据链）

评审常看“是否有清晰 Prompt、迭代、校验和过程记录”。为证明 **人是主导、AI 是副驾驶**，建议对关键任务保留最小证据链（可写进 PR 描述或复盘材料，内容需脱敏）：  

- **Prompt v1（初稿）**：目标/范围/验收/约束是否清晰？  
- **AI 的错误（Bad Case）**：记录 AI 产出的典型错误（例如：编造不存在的 API、DTO 字段错、漏权限校验、信任前端 userId、Query key 过粗导致串数据）。  
- **Refinement（修正指令）**：追加约束并要求重生成（例如：“必须使用 @Roles()；不得信任 userId；必须做组织成员校验；Query key 必须包含 week+user”）。  

建议保存方式（任选其一即可）：  
- **PR 描述**中放“3 段短文本 + 1 张关键截图（脱敏）”。  
- 或在 `prompts/` 下新增**脱敏后的** `case-studies/`（若团队决定这么做，注意不要放敏感数据）。  

#### 可选：AI 贡献度量（不要过度追求精确）

为避免“我们用了 AI 但说不清贡献”，可以在复盘中用简单口径描述（定性 + 轻量定量）：  
- **产出类型**：PRD/接口方案/代码/测试/文档/Review checklist 各占比（主观估计即可）。  
- **迭代轮次**：关键需求平均 Refinement 次数（例如 2～3 轮）。  
- **返工减少点**：举 1～2 个“AI 先踩坑 → 人修正后固化到模板”的例子。

---

## 7. 产出物与比赛材料如何对齐

- **代码**：PR/Commit 记录可追溯；避免无关的大杂烩提交。  
- **模板**：`prompts/` 中保留可复用模板与脱敏示例；迭代见 [`prompts/README.md`](./README.md) 的约定。  
- **说明文档**：`docs/` 或子项目 `docs/` 中补充架构/接口/联调说明，便于新成员与评委跟进。  
- **复盘**：用 `prompts/review.md` 生成比赛复盘要点与 Q&A，再人工补充证据（截图/录屏/PR 链接）。  

为强化“创新加分”的**量化与可视化**，建议在复盘中补 1～2 张对比图（粗略即可，无需精确到小数点）：  
- **时间对比**：同一类任务（如复杂规则抽取/协议解析）历史平均耗时 vs 本次 AI 辅助耗时。  
- **覆盖率**：AI 生成的代码行数占总变更行数的比例（可用“估算区间”，例如 30%～50%）。  
这些数据可作为答辩时的“效能提升”佐证，帮助评委快速理解 AI 的实际价值。

---

## 8. 反模式与常见返工

- **只描述“做个 XXX”**、不给出模块路径与验收，导致多轮确认与风格不一致。  
- **把 .env/密钥/整段生产日志**贴进对话或提交到仓库。  
- **大拆大建**：同一 PR 里混需求变更、大重构、格式化全仓库，难 review、难回滚。  
- **只改前端或只改后端**：契约未对齐就实现，联调时才发现 DTO/状态码不一致。  
- **忽略 Query 维度**：多用户/多周/多视图下 key 过粗，出现界面显示错误数据。  
- **合入前未跑 typecheck/build**：在 CI 或他机才爆编译错误。  

---

## 9. 文档与模板的维护

- 新类型任务若反复使用同类 Prompt，应**固化为** `prompts/*.md` 新文件或更新现有模板，并同步本说明中的链接与清单。  
- 重大架构或目录变更后，**至少**更新本文件第 4 节与 `prompts/README.md` 的「文件一览」表。  
- 本说明侧重「人与 AI 协作的规矩」；**具体技术细节**以代码与 `docs/` 为准，避免与实现漂移。  
- **资产沉淀（赛后归档建议）**：比赛结束后，将 `prompts/*.md`（及脱敏示例、复盘要点）归档至团队/公司知识库，作为“AI 辅助企业级应用开发”的 SOP 初版，再按实际项目持续迭代。

---

## 10. 附录：极简示例 Prompt

本节为**固定化骨架示例**，用于快速开聊；下述方括号/尖括号中内容请换为**真实、已脱敏**信息。更完整的合同级字段、表格与分节，请务必使用 `prompts/` 里对应文件，**勿把本附录当作第二套 PRD**。

| 场景 | 展开请用 |
|------|----------|
| 需求/用户故事/验收 | [`prompts/prd.md`](./prd.md) |
| Nest 接口、DTO、权限、错误 | [`prompts/nest-module.md`](./nest-module.md) |
| 前端 `features`、Query、`apiRequest` | [`prompts/frontend-feature.md`](./frontend-feature.md) |
| 测试用例与断言 | [`prompts/testing.md`](./testing.md) |
| 复盘/答辩 | [`prompts/review.md`](./review.md) |

---

### 10.1 小功能 / 需求迭代（复制后替换）

```text
【目标】<一句话：用户/管理员能完成什么，价值是什么>

【范围】<前端路径或模块>；<后端模块或路由前缀>；<不在范围内请写清>

【权限】<谁可见/谁可操作>；<是否涉跨用户、代操作、多组织>；无则写「同现有 timesheet/项目 模块默认规则」

【验收（至少 3 条，可测）】
1. <操作 + 期望可见结果 / API 行为>
2. <边界：空列表、无权限、已锁定 等，若相关>
3. <错误提示或拦截方式，不泄露实现细节>

【约束】<不修改的文件或不得破坏的 API 行为>；<须跑的命令：如 frontend npm run build>

【参考】<可选：仓库内可照抄的类似文件路径>
```

---

### 10.2 Bug 修复（复制后替换）

```text
【现象】<当前错误行为，用户可见描述；日志/响应码可写但须脱敏>

【期望】<正确行为，一句话 + 边界条件若需要>

【复现】<分支/环境>；<步骤 1→2→3>；<是否必现/间歇>

【已排查】<可选：试过什么、不是什么原因>

【涉及】<合理猜测的目录或文件名，可写「让 AI 先查」但尽量给入口文件>
```

---

### 10.3 新增或调整 API（与 DTO/权限强相关时的开场白）

```text
在遵守仓库 Nest 模块与 DTO 约定的前提下，请按 prompts/nest-module.md 的结构补充完整方案后再改代码。
以下是摘要：
【方法/路径】例如 `GET /api/v1/...`（写清 method 与路径模式）
【用途】<一句话>
【鉴权/角色】<与组织、资源的关系>
【Body/Query 要点】<字段名与类型级描述，可列表>
【错误与 HTTP 码】<用户可见的英文/中文提示约定>
【参考模块】<backend 内已有类似 endpoint 路径>
```

完整字段表、错误码与示例请求仍以 [`nest-module.md`](./nest-module.md) 为准。

---

### 10.4 纯前端某页/交互（开场白）

```text
请按 prompts/frontend-feature.md 的约定，在 <frontend/src/features/某域/> 下实现/修改。
【页面/入口】<路由或从哪个菜单进入>
【数据】<调用的 list/get/mutation 语义；Query key 须包含的维度>
【不改动】<不碰的模块或只读接口>
【验收】<可截图或手工点的 2～3 条>
```

细化的组件拆分、Loading/空态/错误，用 [`frontend-feature.md`](./frontend-feature.md) 补全。

---

### 10.5 复杂业务 / 协议解析（比赛专用示例）

```text
【任务类型】协议解析 / 遗留系统复刻 / 复杂业务规则抽取

【输入源】
1) <贴出脱敏后的原文片段 / 规则描述 / 页面 DOM 结构 / 截图文字转写>
2) <如有：相关已有代码路径，便于对齐现状>

【目标输出】
- 输出 TypeScript 的 interface / type（字段名、类型、可选性）
- 输出状态映射关系（例如：状态机、审批流、权限矩阵），并明确每条规则的来源句子

【约束（强制）】
1) 严禁编造：原文未提及的字段/枚举/接口，请标注为 UNKNOWN 或 TODO，并说明缺失信息是什么。
2) 若有枚举值/编码（例如十六进制/字符串 code），保留原始值，不要“猜含义”。
3) 若存在安全/权限规则，必须明确：允许/禁止/条件，且列出反例（越权应如何被拦截）。
4) 输出中请分开：事实（来自原文） vs 推断（需人工确认）。
5) **严禁伪实现**：如果无法生成确定性的解析逻辑（例如复杂位运算/加密/校验算法），禁止用看似合理的伪代码“填满”。必须输出 `// TODO: <需人工实现的复杂逻辑>` 并停止，等待人工介入确认。

【输出格式】
- 先给“结论摘要”（3～5 条）
- 再给 TypeScript 代码块（interface/type）
- 最后给一份“待确认问题清单”（用于下一轮 Refinement）
```

---

*最后更新：随仓库迭代；附录仅作“起手式”示例，正式交付以 `prompts/*.md` 与代码为准。*